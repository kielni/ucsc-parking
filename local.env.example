# Copy to local.env (gitignored) and fill in real values.
# Read by the Makefile (via -include) for the build/deploy/run-aws targets.

# lambda_handler.py (set these on the Lambda function too)
S3_BUCKET=

# Makefile: build / deploy
AWS_ACCOUNT_ID=
AWS_REGION=us-west-2
ECR_REPO=
IMAGE_NAME=parking-map
FUNCTION_NAME=
